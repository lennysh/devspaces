# This file contains ignores rule violations for ansible-lint
# Note: playbooks and roles/devspace/meta/main.yml are excluded in .ansible-lint exclude_paths
# to avoid syntax-check errors for collection roles that aren't available until the collection is built
